name: Deploy
on:
  workflow_dispatch:
  push:
    branches: [master]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
      - run: npm ci
      - run: cd app && npm ci && npx ng build --configuration production
      - run: npx wrangler@latest deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.SECRET_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.SECRET_TOKEN }}